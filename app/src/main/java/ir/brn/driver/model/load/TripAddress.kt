package ir.brn.driver.model.load

import ir.brn.driver.model.core.City

data class TripAddress(val id: Int?,
                       val title: String?,
                       val address: String?,
                       val city: City?)